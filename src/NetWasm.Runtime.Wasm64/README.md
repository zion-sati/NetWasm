# NetWasm.Runtime.Wasm64

`NetWasm.Runtime.Wasm64` provides the managed CoreLib and runtime ABI assets
for NetWasm `wasm64` applications. The NetWasm SDK selects this package when
`NetWasmTarget=wasm64`.

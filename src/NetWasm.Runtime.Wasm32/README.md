# NetWasm.Runtime.Wasm32

`NetWasm.Runtime.Wasm32` provides the managed CoreLib and runtime ABI assets
for NetWasm `wasm32` applications. The NetWasm SDK selects this package when
`NetWasmTarget=wasm32`.
